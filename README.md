بفرما! این کل محتویات فایل **`README.md`** است که به‌روز شده و آماده کپی/پیست می‌باشد:


# HalfSpace & Otomatn | اصلاح‌گر هوشمند متن فارسی

این پروژه شامل یک کتابخانه قدرتمند جاوااسکریپت و افزونه‌های مرورگر (با نام تجاری **اتومتن**) است که به شما کمک می‌کند متون فارسی را استانداردسازی کنید. این ابزار به صورت خودکار نیم‌فاصله‌ها را اصلاح می‌کند، فاصله‌های اضافی علائم نگارشی را حذف می‌کند و ساختار افعال را بهبود می‌بخشد.

---

## 📦 افزونه‌های مرورگر (اتومتن)

اکنون می‌توانید از قدرت این کتابخانه در مرورگرهای خود استفاده کنید. افزونه **اتومتن (Otomatn)** به شما اجازه می‌دهد در هر وب‌سایتی متن را انتخاب کرده و با یک کلیک آن را ویرایش کنید.

### نصب دستی (Developer Mode):
1. پروژه را دانلود کنید (Download Zip) یا کلون کنید.
2. **برای کروم:** به مسیر `chrome://extensions` بروید، حالت **Developer Mode** را (بالا سمت راست) روشن کنید و دکمه **Load Unpacked** را بزنید، سپس پوشه پروژه را انتخاب کنید.
3. **برای فایرفاکس:** به مسیر `about:debugging` بروید، روی **This Firefox** کلیک کنید و فایل `manifest.json` را بارگذاری کنید.

---

## 💻 راهنمای توسعه‌دهندگان (کتابخانه JS)

اگر توسعه‌دهنده وب هستید و می‌خواهید این قابلیت را به وب‌سایت یا پروژه خود اضافه کنید، می‌توانید از فایل اصلی کتابخانه استفاده کنید.

### نصب

کافی است فایل جاوااسکریپت را به پروژه خود اضافه کنید یا از CDN استفاده کنید:

```html
<script src="HalfSpace.js" type="text/javascript"></script>

<script src="[https://cdn.jsdelivr.net/gh/mr-shady/halfspace/FixGrammar.min.js](https://cdn.jsdelivr.net/gh/mr-shady/halfspace/FixGrammar.min.js)" type="text/javascript"></script>

```

### نحوه استفاده

**روش اول (اصلاح یک المنت خاص):**

```javascript
// اصلاح متن داخل یک المنت با ID مشخص
CheckGrammar('#Grammer');

```

**روش دوم (استفاده مستقیم در کد):**

```javascript
let rawText = 'این متن نیاز به اصلاح نیم فاصله دارد.';
const fixer = new FixGrammar(rawText);
let cleanText = fixer.get();

console.log(cleanText); 
// خروجی: این متن نیاز به اصلاح نیم‌فاصله دارد.

```

---

## ✨ قابلیت‌ها

این ابزار موارد زیر را به صورت خودکار شناسایی و اصلاح می‌کند:

* **علائم نگارشی:** اصلاح فاصله قبل و بعد از `.`، `،`، `:`، `؛`، `!`، `؟` و ...
* **پسوندهای "ی":** در کلماتی مانند: *جمله‌ی، برنامه‌ی، فاصله‌ی*.
* **نشانه‌های جمع "ها":** در کلماتی مانند: *درخت‌ها، کتاب‌ها، خانه‌ها، بانک‌های*.
* **افعال و پسوندها:** اصلاح ساختار در افعالی مثل: *رفته‌ام، آمده‌اند، گرفته‌ای*.
* **پیشوندهای افعال (می/نمی):** اصلاح نیم‌فاصله در: *می‌توانم، می‌رود، نمی‌خرند، نمی‌شود*.
* **کلمات مرکب:** پشتیبانی از کلماتی مثل *گل‌گفتن، گل‌شنیدن* (در حال تکمیل).

### موارد در دست توسعه (Roadmap)

* بهبود تشخیص افعال متصل که باید جدا شوند (تبدیل "میشود" به "می‌شود").
* تکمیل دیتابیس کلمات مرکب خاص.

---

## 📸 تصاویر (Screenshots)

**قبل از اصلاح:**

![Before](https://github.com/mr-shady/halfspace/raw/main/Screenshot.png) 

**بعد از اصلاح:**

![After](https://github.com/mr-shady/halfspace/raw/main/Screenshot-after.png) 

---

## 🔒 حریم خصوصی (Privacy)

این افزونه **امن و آفلاین** است. تمام پردازش‌ها داخل مرورگر کاربر انجام شده و هیچ داده‌ای ذخیره یا ارسال نمی‌شود. برای جزئیات بیشتر فایل [PRIVACY.md](PRIVACY.md) را مطالعه کنید.

## ✉️ بازخورد

اگر پیشنهاد، انتقاد یا باگی پیدا کردید، خوشحال می‌شوم از طریق ایمیل زیر با من در ارتباط باشید:

📧 reza.shady@gmail.com

## 📄 لایسنس

این پروژه تحت لایسنس [MIT](https://choosealicense.com/licenses/mit/) منتشر شده است.

